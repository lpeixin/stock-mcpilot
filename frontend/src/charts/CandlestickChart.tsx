import React from 'react'
// Placeholder for future ECharts K-line component
const CandlestickChart: React.FC<{ symbol: string }> = ({ symbol }) => {
  return <div className="h-64 flex items-center justify-center text-xs text-gray-400 border rounded">K线图占位: {symbol}</div>
}
export default CandlestickChart
