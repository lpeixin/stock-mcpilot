# LLM 模式: local 或 cloud
LLM_MODE=local
# 云端模型 Key (cloud 模式需要)
LLM_API_KEY=
# 本地模型名称 (例如 llama3, qwen 等，根据本地推理环境)
LLM_LOCAL_MODEL=llama3

# 数据库 (默认 SQLite)
DATABASE_URL=sqlite:///./backend/storage/stock_cache.db
